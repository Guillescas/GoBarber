import CreateAppointmentService from './CreateAppointmentService';

describe('CreateAppointment', () => {
  it('Should be able to reate a new appointment', () => {
    expect(1 + 2).toBe(3);
  });

  it('Should not be able to reate two appointments on the same time', () => {
    expect(1 + 2).toBe(3);
  });
});
